<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.ProjectTest.TalentMapper">
	<insert id="talentAdd" parameterType="Talent">
		<!-- 더미 테이터 떄문에 시퀀스x MAX사용 -->
		<!-- SELECT talent_seq.NEXTVAL FROM dual -->
		INSERT INTO talent VALUES((SELECT MAX(talent_no)+1 FROM talent), #{sellerId}, #{upperCategoryNo}, #{lowerCategoryNo}, 
					#{mainImg}, #{title}, #{content}, #{bamboo}, #{saleBamboo}, #{summary}, '검토중', 0, SYSDATE, NULL)
	</insert>
	<select id="getTalent" resultType="Talent" parameterType="Long">
		SELECT * FROM talent WHERE talent_no = #{talentNo}
	</select>
	<update id="talentModify" parameterType="Talent">
		UPDATE talent SET upper_category_no = #{upperCategoryNo}, lower_category_no = #{lowerCategoryNo}, main_img = #{mainImg},
					title = #{title}, content = #{content}, bamboo = #{bamboo}, sale_bamboo = #{saleBamboo}, summary = #{summary}
					WHERE talent_no = #{talentNo}
	</update>
</mapper>