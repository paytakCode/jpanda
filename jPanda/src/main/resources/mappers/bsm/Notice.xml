<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.ProjectTest.NoticeMapper">
	<select id="selectNoticeCountByPager" resultType="int" parameterType="RegistPager">
		SELECT 	COUNT(*)
		FROM	notice
		<if test="search != '' and search != null">
			WHERE	title LIKE '%' || #{search} || '%'
			OR 		DBMS_LOB.INSTR((n.content,'%' || #{search} || '%') > 0
		</if>
		
	</select>
	<select id="selectNoticesByPager" resultType="RegistNotice" parameterType="RegistPager">
		SELECT 	*
		FROM 	(SELECT	ROWNUM rn, n.*
				 FROM	(SELECT	  * 
				 		 FROM 	  notice 
				 		 ORDER BY reg_date DESC) n
				 <if test="search != '' and search != null">
				 WHERE 	n.title LIKE '%' || #{search} || '%' 
				 OR 	DBMS_LOB.INSTR((n.content,'%' || #{search} || '%') > 0
				 </if>
				)
		WHERE  	rn BETWEEN #{startRow} AND #{endRow}
	</select>
	<select id="selectNoticeByNoticeNoRegist" resultType="RegistNotice" parameterType="Long">
		SELECT	*
		FROM	notice
		WHERE	notice_no = #{noticeNo}
	</select>
</mapper>