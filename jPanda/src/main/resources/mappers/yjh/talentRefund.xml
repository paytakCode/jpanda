<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kakao.jPanda.yjh.TalentRefundMapper">

	<select id="selectTalentRefund" parameterType="List" resultType="TalentRefund">
		select	
				bu.buyer_id, tr.purchase_no, bu.talent_no, tr.refund_bamboo, tr.issue, 
        		tr.refund_status, tr.approve_date
		from 	talent_refund tr, bamboo_use bu
		where	tr.purchase_no = bu.purchase_no
		and		tr.refund_status = '검토중'
	</select>
	
	<update id="updateTalentRefundToSuccessByPurchaseNosAndStatus" parameterType="TalentRefund">
		update 	talent_refund
		set		refund_status = #{refundStatus},
				approve_date = sysdate
		where	purchase_no in #{purchaseNo}	
	</update>
	
	<update id="updateTalentRefundToCompanionByPurchaseNosAndStatus" parameterType="TalentRefund">
		update 	talent_refund
		set		refund_status = #{refundStatus}
		where	purchase_no in #{purchaseNo}
	</update>

</mapper>