<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kakao.jPanda.yjh.TalentRefundMapper">

	<select id="selectTalentRefund" parameterType="List" resultType="TalentRefund">
		select	
				bu.buyer_id, tr.purchase_no, bu.talent_no, tr.refund_bamboo, tr.issue, 
        		tr.status, tr.approve_date
		from 	talent_refund tr, bamboo_use bu
		where	tr.purchase_no = bu.purchase_no
		and		tr.status = '검토중'
	</select>
	
	<update id="updateTalentRefundToSuccessByPurchaseNosAndStatus" parameterType="List">
		update 	talent_refund
		set		status = 
						<foreach collection="list" item="talentRefundDto" open="(" close=")" separator=",">
    						#{status}
  						</foreach>,
				approve_date = sysdate
		where	purchase_no in
							<foreach collection="list" item="talentRefundDto" open="(" close=")" separator=",">
    							#{purchaseNo}
  							</foreach>
				
	</update>
	
		<update id="updateTalentRefundToCompanionByPurchaseNosAndStatus" parameterType="List">
		update 	talent_refund
		set		status = 
						<foreach collection="list" item="talentRefundDto" open="(" close=")" separator=",">
    						#{status}
  						</foreach>
		where	purchase_no in
							<foreach collection="list" item="talentRefundDto" open="(" close=")" separator=",">
    							#{purchaseNo}
  							</foreach>
				
	</update>

</mapper>