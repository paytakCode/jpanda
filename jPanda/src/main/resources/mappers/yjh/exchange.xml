<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kakao.jPanda.yjh.ExchangeMapper">
	<select id="ExchangedList" parameterType="Exchange" resultType="Exchange">
		select exchange_No, talent_No, id, submit_Date, total, status
		from exchange
		where status = '검토중'
	</select>
	
	<update id="ExchangedUpdate" parameterType="java.util.List">
 		update exchange
  		set status = '환전완료',
      	approve_date = sysdate
  		where exchange_No in
  			<foreach collection="list" item="listExn" open="(" close=")" separator=",">
    			#{listExn}
  			</foreach>
	</update>
	
	<update id="ExchangeUpdateToCompanion" parameterType="java.util.List">
		update exchange
  		set status = '반려',
      	approve_date = sysdate
  		where exchange_No in
  			<foreach collection="list" item="longListExhangeNo" open="(" close=")" separator=",">
    			#{longListExhangeNo}
  			</foreach>
	</update>
</mapper>