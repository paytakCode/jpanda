<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kakao.jPanda.yjh.ExchangeMapper">

	<select id="selectExchange" resultType="AdminExchange">
		select 	exchange_No, talent_No, seller_id, submit_Date, total, exchange_status
		from 	exchange
		where 	exchange_status = '검토중'
	</select>
	
	<select id="selectExchangeByExchangeNo" parameterType="Long" resultType="AdminExchange">
		select 	* 
		from 	exchange
		where 	exchange_no = #{exchangeNo}
	</select>
	
	<update id="updateExchange" parameterType="AdminExchange">
		update 	exchange
		set 	exchange_status = #{exchangeStatus},
				approve_date = sysdate
		where 	exchange_no = #{exchangeNo}
	</update>
	
</mapper>